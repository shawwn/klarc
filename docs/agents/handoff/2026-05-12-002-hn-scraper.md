---
name: HN scraper (scrape.arc + json.arc)
description: Scrapes the live HN front page and item pages for flagged/dead/collapsed comment info the API doesn't expose. Logs in as `hnscraper`, fetches `topstories.json` for ordering, scrapes each item's HTML for comments + status flags, hits Firebase API for user profiles. Stores JSON under `arc/scrape/` and provides `import-scrape!` to populate News's in-memory tables.
type: project
---

# Handoff: HN scraper (2026-05-12)

## What landed

Two new files + small extensions:

- `scrape.arc` (525 lines) -- HTTP via curl, HTML parser for HN's
  `<tr class="athing comtr">` rows, top-level `scrape!` and
  `import-scrape!` entry points.
- `json.arc` (212 lines) -- standalone JSON encoder + decoder used by
  the scraper.  No external deps.
- `test.arc` -- 9 new `define-test json-*` blocks (encode/decode
  primitives, escapes, arrays, objects, roundtrip).  Suite went from
  241 to 291 passing, 0 failing.
- `arc0.lisp:659-666` -- one-line change: `pipe-from` now uses
  `:external-format :latin-1`.  Without this, subprocess stdout was
  decoded as UTF-8 (SBCL default), then writing those chars to
  Latin-1 output files (the `outfile` default) failed on any
  non-Latin-1 codepoint like U+201C left-double-quote.  Latin-1
  byte-pass-through everywhere keeps `curl | parse | save` clean.
- `scrape-verify-flags.arc` -- assertion script that re-fetches
  the 4 context items from `docs/agents/prompt.txt` and asserts each
  named comment has the expected `dead`/`flagged`/`collapsed`
  combination.  Run with `./sharc scrape-verify-flags.arc`.

## Usage

```sh
./sharc                          # then at the REPL:
arc> (load "scrape.arc")
arc> (scrape!)                   ; full run, ~60 items (default limit)
arc> (scrape! nil 5)             ; dev: just the top 5
arc> (scrape! t)                 ; force re-scrape even if cached
```

Then to mirror HN on the local News server:

```arc
arc> (load "news.arc")
arc> (load "scrape.arc")
arc> (import-scrape!)            ; populates stories*/items*/profs*
arc> (asv 8080)                  ; server now serves HN's front page
```

## Data layout

Everything writes under `arc/scrape/`:

- `cookies.txt` -- curl cookie jar (HN session for `hnscraper`).
  Login is automatic via `ensure-login` whenever the session is
  invalid; credentials are hardcoded as defaults to `hn-login`.
- `last-fetch.lisp` -- table of `id -> unix-seconds`.  An item id
  recently fetched (< `scrape-refetch-secs*`, default 3600) is
  skipped on the next pass.  User entries use the key `(sym "u/" id)`.
- `front.json` -- last scrape's ordered story list: `[{id, page, rank}, ...]`.
  Read by `import-scrape!` to reconstruct HN's order.
- `item/{id}.json` -- merged story + comment tree for one item.
- `user/{id}.json` -- one user profile (from Firebase API).

The per-item JSON shape:

```json
{ "story": {"id":..., "type":"story", "by":..., "title":..., "score":...,
            "url":..., "text":..., "time":..., "dead":..., "fetched_at":...},
  "comments": [ {"id":..., "by":..., "time":..., "parent":..., "indent":...,
                 "text":..., "dead":..., "flagged":..., "collapsed":...,
                 "deleted":..., "descendants":...},
                ... ] }
```

## Key decisions

### 1. Front-page ordering from the API, not HTML

The HN Firebase API's `/v0/topstories.json` returns up to 500 ranked
story ids in one call.  That's authoritative and replaces parsing
`/news` + `/news?p=2`.

The original spec said "scrape the front page ourselves", but the
user later clarified that the API is more faithful at any given
moment, and per-item HTML scraping (which is still needed for
dead/flagged/collapsed comment info) covers the use-case.  The old
HTML-front-page parser code was deleted.

### 2. Per-item HTML is the only source for comment status

Firebase's `/v0/item/{id}.json` exposes `dead: true` for dead
comments but **not** `[flagged]`-only state or `[collapsed]` state.
So per-item HTML is mandatory for the scraper's stated purpose.

The user was explicit: **do not use the API for items/comments**.
Only for users.  `fetch-user` is the only API call besides
`topstories.json`.

### 3. Crawl-delay = 3s, overriding robots.txt's 30s

`hnscraper`'s About page invites contact rather than ban, and the
user explicitly authorized the faster rate ("you can also scrape
every few seconds instead of every 30s.  Maybe 3s").  Hard-coded as
`scrape-crawl-delay*` at the top of scrape.arc with a comment
explaining the deviation.  Revert to 30 if HN ops complains.

There's no `(fetch-robots-delay)` anymore -- it was removed in the
cleanup pass once the value was hardcoded.

### 4. Per-comment HTML scoping (perf)

The first version of `parse-comments` called `posmatch` on the full
2MB html for each of ~1000 comment-row fields.  That's O(C*N) and
hung for minutes on a real item page.

The fix is in `parse-comments`: walk the full html once to collect
the byte offsets of every `<tr class="athing comtr` anchor, then for
each comment cut the substring between consecutive anchors and pass
**that** (~1KB per row) to `parse-comment-row`.  All subsequent
posmatches scan within the row's substring.  Drops parse time from
"hangs >5min" to <1s for a 2MB item page.

### 5. Parent-id derivation from indent stack

HN's HTML omits the `parent` id on comment rows (it's only on the
permalink "context" view).  But comments are rendered in DFS order
with `<td class="ind" indent="N">` carrying the depth.  So we keep
an `indent-stack` table mapping `level -> id-of-most-recent-row`,
and a comment at indent N has parent = `indent-stack[N-1]` (or the
story id if N=0).  Wipe deeper-level entries each time we descend.

### 6. Deletion-aware comment merge

When refetching an item, `build-item-json` merges the new comments
with the previously-saved set: if an id existed before but is
missing from the new fetch, it's kept with `deleted: true`.  Order
of merged output is new-fetch-first, then the now-deleted entries
appended at the end.  The user asked for this explicitly.

### 7. JSON-decoded `type` is a *string*, not a symbol

The encoder turns Arc symbols into JSON strings; on decode they come
back as strings.  News's `displayfn*` table is keyed by symbols, so
`(displayfn* "story")` returns nil and rendering crashes with the
unhelpful "Function call on non-function: NIL".  Fix is in
`import-scraped-story`: `'type (sym (or s!type "story"))`.

`import-scraped-comment` hardcodes `'type 'comment`, so it doesn't
have the same problem.

### 7b. Persisted items must contain only readable values

News's `save-item` does `(write (tablist i))` to disk and `load-item`
reads it back via `(read)`.  Every value in an item's table has to
print in a form that `read` can re-parse: lists, atoms, strings,
ints, symbols, chars, nil/t.

**Hash tables, functions, streams, and other opaque objects print as
`#<...>` and silently break the roundtrip** -- the file looks fine
on disk, but the next `load-item` raises a read error.  This bit us
once: the original `(load-scrape-config)!username` form was wrong
(the `!` ssyntax doesn't bind across a `)`), so `scrape-flagger*`
resolved to the config table itself.  That table then went into
`i!flags` and onto disk as `(flags (#<HASH-TABLE ...>))`.

If you add a new field to imported items, make sure its value is
readable -- coerce or extract before stashing if there's any doubt.

### 8. Imported `[flagged]` comments populate `i!flags`

News's `(flagged i)` returns t iff `(len> i!flags many-flags*)` and
the item is alive.  `i!flags` is a list of *usernames-as-symbols*
(each entry is who flagged the comment).

When importing a `[flagged]` comment we record the scraper account
as the flagger, the *symbol* form -- `'hnscraper`, not `"hnscraper"`
-- to match the type News writes there.  And we record it
`(many-flags* + 1)` times so that the length-strictly-greater-than
check passes regardless of what `many-flags*` happens to be set to
in the running News.  Implementation lives in `scrape-flag-list` and
`import-scraped-comment`.

`import-scrape!` also lowers `many-flags*` to 0 (so flagged comments
show up under News's `/flagged` view), but the comment-side logic
above doesn't *depend* on that change -- it's belt-and-suspenders.

(Dead+flagged comments still won't show as "flagged" in News's
sense: `(flagged i)` also requires `(live i)`, and a dead comment
isn't live.  That matches News's semantics -- a dead comment moves
from the flagged bucket to the killed bucket.)

## Gotchas discovered along the way

### Arc macro-vs-local-var shadowing

A local variable named `out` or `row` silently breaks anywhere it's
used in head-of-form position, because Arc's compile-time
macroexpansion runs on `(var ...)` calls regardless of local
bindings.  `out` is `(mac out (expr) ... (eval expr) ...)` in
arc.arc; `row` is `(mac row args ... `(tr ...))` in html.arc.
Symptoms:

- `out` shadowing: compile-time `(eval some-local)` -> "Unbound
  variable" error during loading.
- `row` shadowing: runtime emits stray `<tr><td>fieldname</td></tr>`
  per access, because `(row 'indent)` expands to `(tr (td 'indent))`.

Variables in scrape.arc were renamed `out -> rec` and `row -> c` /
`entry`.  Other shadowing-prone names to avoid: `tr`, `td`, `tdr`,
`trtd`, `tab`, `form`, `center`, `link`, `but`, `input`, `br`,
`tag`.

### Bracket-lambda + `!` ssyntax footgun

`[_!dead]` does **not** mean `(fn (_) (_ 'dead))`.  Bracket lambdas
always wrap the body in a call form, so it expands to
`(fn (_) (_!dead))` = `(fn (_) ((_ 'dead)))` -- it calls the
*result* as a function, which blows up if it's nil/t/anything
non-callable.

Workaround: use `(fn (x) x!dead)` or `[_ 'dead]`.  Multi-token
bracket bodies are fine: `[item _!id]` -> `(fn (_) (item _!id))` ->
`(fn (_) (item (_ 'id)))` works.

### `(/ a b)` returns a rational, not an int

`(mod (/ 31 16) 16)` -> `(mod 31/16 16)` -> still a rational, not
indexable into a string.  Use `(trunc (/ n 16))` for integer
division.  Bit me in `json-write-string`'s \uXXXX path.

### Arc's `loop` and `cond`

- `cond` does **not** exist in Arc.  Use `(if t1 r1 t2 r2 ... default)`.
- `loop` is `(loop init test update body)`, not `(loop ((forever))
  ...)`.  Use `(while t ...)` or `(catch (while t ... (throw)))` for
  early exit instead.
- `(return X)` doesn't exist.  Use `(catch ... (throw X))`.

### Arc's `let` is single-binding

`(let a 1 b 2 ...)` is `(let a 1 b 2 ...)` parsed as binding `a` to
`1` and `b` to `2`... wait no, `let` only takes one binding.  For
multiple, use `with` (parallel) or `withs` (sequential).  Bit me in
`parse-item-page` where I wrote `(let story (...) comments (...) ...)`.

### `writefile` Lisp-escapes string values

`(writefile some-html-string path)` writes `"escaped contents"`, not
raw bytes.  For raw output use `w/outfile o path (disp val o)`.  My
JSON path uses `w/outfile` directly and is fine; HTML caching was
never necessary so I removed it.

## Status of the four spec test cases

All four pass via `scrape-verify-flags.arc`:

| comment id | context | expected | actual |
|---|---|---|---|
| 48106273 | 48085993 | [flagged][dead] | ✓ dead=t flagged=t |
| 48105810 | 48038191 | [dead] | ✓ dead=t |
| 48092598 | 48086190 | [flagged][dead][collapsed] | ✓ all three |
| 48085067 | 48073201 | [collapsed] live | ✓ collapsed only |

## End-to-end verification

```
$ ./sharc
arc> (load "news.arc") (load "scrape.arc")
arc> (scrape! nil 3)         ; fetches 3 items at 3s/each
arc> (import-scrape!)        ; imported 3 stories
arc> (asv 8081)
... ready to serve port 8081
$ curl -s http://127.0.0.1:8081/ | grep -oE 'Learning Software|110 points|user\?id=surprisetalk'
Learning Software Architecture
110 points
user?id=surprisetalk
```

## Performance: user-fetch parallelism

The initial implementation called `scrape-user!` sequentially: 263
users x ~1s/curl = ~5 min for a single full scrape.  Two changes
brought that down to ~20-30s:

### 1. `SHARC_HEAP_MB` default 32 GB -> 2 GB (`sharc` script)

`sb-ext:run-program` does fork+exec, and on macOS the forked child
inherits the parent's virtual address space.  At 32 GB that fork is
slow.  Visible in the scraper as: 10 parallel `(shell 'sleep 1)`
calls took 6s instead of 1s.  Lowering the heap to 2 GB ran the
same workload at native speed.

The old 32 GB default matched HN production; serious workloads can
still opt in via `SHARC_HEAP_MB=32768 ./sharc`.

### 2. Batch curls inside one shell with `&` + `wait`

Even after the heap fix, Arc threads + SBCL `run-program`-per-curl
was ~15x slower than native shell job control because each
`run-program` invocation has per-process startup overhead.  The
fix is in `scrape-user-batch!` (`scrape.arc:475`): build one shell
command of the form

```
curl ... -o u1.json.raw & curl ... -o u2.json.raw & ... & wait
```

so SBCL only spawns *one* subprocess per batch.  The shell does
the fan-out internally.  Batch size is `scrape-user-concurrency*`
(default 10).

### 3. Skip JSON parse on the scrape path

`from-json` on a 100KB pg user object (long `submitted` array)
takes ~0.4s in pure Arc.  Worse, running it in 10 threads
concurrently *thrashes* the allocator/GC -- parallel was slower
than sequential.

Fix: `scrape-user!` no longer parses+reserialises.  It saves the
firebase response raw and uses `inject-fetched-at` (`scrape.arc:421`)
to splice `,"fetched_at":<t>` in just before the trailing `}` with
pure string operations (no `trim` -- that allocates a copy of the
whole 100KB string per call, which itself was the next perf cliff).

### Numbers, 20 known users force-refetched

| approach                                          | time |
|---|---|
| native `bash` baseline (`curl ... &`)             | 0.65s |
| sequential `scrape-user!`                         | ~20s |
| Arc threads + semaphore (before heap fix)         | 60s+ |
| Arc threads + semaphore (after heap fix)          | 8-9s |
| Batched single-shell (current)                    | **3s** |

For a real 263-user import, that translates to roughly 20-30s for
the user-fetch phase, instead of the original 5 minutes.

## Future perf work

- `import-scrape!` still parses every saved user JSON via the
  pure-Arc `from-json` (~0.4s for big users -> ~100s for 263 users).
  Acceptable for a once-per-import cost; would benefit from a native
  CL parser (jzon via quicklisp, or ~150 lines of CL in `arc1.lisp`).
  The `from-json` / `load-json` API can stay the same.

## Authentication

Credentials are no longer hardcoded.  `scrape.json` (gitignored)
holds the username and optionally the password.  A committed
`scrape.example.json` template provides the default username
(`hnscraper`); on first run the code copies the example into place.

`ensure-login` checks three sources in order:

1. **Existing valid `arc/scrape/cookies.txt`** -- if it works, skip
   everything.
2. **Pre-baked cookie** from `HN_SCRAPER_COOKIE` env or `"cookie"`
   field in `scrape.json` -- value is the raw `<user>&<token>` string
   from the browser's `user` cookie.  Written into the Netscape jar
   via `write-cookie-file`, then re-validated.  Lets you bring a
   browser session in without ever typing a password.
3. **Password login** via `hn-login`.  Password resolution order:
   1. `HN_SCRAPER_PASSWORD` env var
   2. `"password"` field in `scrape.json`
   3. No-echo interactive prompt (uses `stty -echo`; errors cleanly
      if stdin is not a TTY)

A prompted password is persisted to `scrape.json` only after the
login actually succeeds, so typos don't stick.  Env-supplied passwords
and cookies are never persisted.

### New primitive: `(getenv NAME &optional DEFAULT)`

Added in `arc0.lisp` near the other system-call wrappers.  Returns
the value of the env var as a string, or `DEFAULT` (or nil) if the
variable is unset *or* set to the empty string.

The empty-as-missing behaviour matches shell's `${VAR:-default}`,
which is what most callers expect.  Callers that really need to
distinguish "explicitly set to empty" from "unset" can drop down
to `sb-ext:posix-getenv` directly.

Examples:

```arc
(getenv "HOME")                        ; -> "/Users/shawn"
(getenv "DOES_NOT_EXIST")              ; -> nil
(getenv "DOES_NOT_EXIST" "fallback")   ; -> "fallback"
(getenv "EMPTY_VAR" "fallback")        ; -> "fallback"  (empty == missing)
```

## Notable concerns

- **News id collisions.**  `import-scrape!` writes HN ids directly
  into `arc/news/story/`.  If you have local items at the same ids,
  they'll be overwritten.  Fine on a fresh install, dangerous
  otherwise.  Could namespace under a separate `arc/news/hn/`
  hierarchy if needed.
- **`maxid*` gap.**  After import, `maxid*` jumps to ~48M.  The next
  locally-created item starts from there, which makes id sequencing
  visually weird but functionally fine.
- **No item-level dead flag.**  We rely on `topstories.json`, which
  already filters out dead stories.  Front-page-level `[dead]` /
  `[flagged]` story status (rare; mostly affects newest/show pages)
  is not captured.  If you need that, you'd have to re-add an HTML
  front-page parser for `/news` (deleted in this pass), or shift
  source to `/newest`.

## Related

- The original prompt is in `docs/agents/prompt.txt`.
- The `hnscraper` account on news.ycombinator.com is the dedicated
  scraping identity.  Its About page invites contact if rate is too
  aggressive; current `Crawl-delay` deviation (3s vs robots.txt's
  30s) is owner-authorized.
- This builds on the existing `system` / `pipe-from` primitives in
  `arc0.lisp:650-666`; the only runtime change there is the
  `:external-format :latin-1` for pipe-from (see decision #0 above).
